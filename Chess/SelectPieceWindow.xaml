<Window x:Class="Chess.SelectPieceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chess"
        mc:Ignorable="d"
        Title="SelectPieceWindow" Height="150" Width="400" WindowStyle="None">
    <Grid x:Name="mainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border BorderBrush="Black" BorderThickness="4" Grid.Column="0">
            <Image x:Name="NKnight"></Image>
        </Border>
        <Border BorderBrush="Black" BorderThickness="4" Grid.Column="1">
            <Image x:Name="Bishop"></Image>
        </Border>
        <Border BorderBrush="Black" BorderThickness="4" Grid.Column="2">
            <Image x:Name="Rook"></Image>
        </Border>
        <Border BorderBrush="Black" BorderThickness="4" Grid.Column="3">
            <Image x:Name="Queen"></Image>
        </Border>
    </Grid>
</Window>